<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="resource.UpsoMapper">
<!--
	<select id="searchUpso" parameterType="vo.SearchVO">
		select *
		from upso
		where upso_nm like '%'||#{keyword1}||'%' and site_addr like '%'||#{keyword2}||'%'

		union

		select *
		from upso
		where upso_nm like '%'||#{keyword2}||'%' and site_addr like		'%'||#{keyword1}||'%'
	</select>
	
	<select id="searchUpsoone" parameterType="vo.SearchVO">
		select *
		from upso
		where upso_nm like '%'||#{keyword1}||'%'
	</select>
	  -->
	  
	<select id="simularsearch"  resultType="Vo.SearchVO" parameterType="String">
		select upso_nm, site_addr
		from (
    		select rownum, upso_nm, site_addr
    		from biz_a
    		where UPSO_NM like '%'||#{keyword}||'%'
		)
		where rownum BETWEEN 1 AND 10
	</select>


</mapper>
